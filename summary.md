# 开发记录

## 日期：2026-08-08

完成：v0.2.9 修复更新下载进度显示——多线程分块下载失败重试时字节数被重复累计，导致进度显示 102%；改为只有成功的分块才计入进度，进度条与文字均封顶 100%。analyze 零问题、29 测试通过，APK v0.2.9（versionCode 11）已打包。

影响：更新下载进度显示恢复正常（文件完整性不受影响，仍有 SHA256 校验兜底）。

备注：——

## 日期：2026-08-08

完成：局域网同步改用「专用同步令牌」（安全改进）——桌面端 lan-sync 启动时生成 32 位随机令牌（data/lan_sync_token.txt，已 gitignore，可删除重新生成）；手机端同步时填写令牌并安全存储，不再用 API Key 明文传输。桌面端 2 项测试 + 手机端 29 项测试全过，APK v0.2.8 重新打包。

影响：即使局域网被监听，泄露的也只是只读用量令牌，不影响 DeepSeek API Key 与余额。

备注：——

## 日期：2026-08-08

完成：局域网一键同步 Codex 用量（保持版本号 v0.2.8）——桌面端新增 lan-sync 服务（0.0.0.0:8002，/api/codex-usage，Bearer 鉴权，只读）；手机端用量页新增「局域网同步」入口（输入电脑 IP，用账户 Key 鉴权，拉取后增量去重入库）。桌面端 2 项服务测试 + 手机端 29 项测试全过，APK v0.2.8 重新打包。

影响：同一 WiFi 下手机一键拉取最新 Codex 用量，接近准实时。

备注：Windows 首次启动 lan-sync 可能弹防火墙授权，需允许；电脑 IP 用 ipconfig 查看。

## 日期：2026-08-08

完成：手机端「导入 Codex 用量」——用量页新增导入入口（粘贴 JSON / 选择文件），解析桌面端 codex-usage --export 的 JSON（输入/命中缓存/未命中缓存/输出/总 Token、线程名），按 codex:key 增量去重写入本地库，图表即时更新；新增 3 项导入解析/去重测试（共 28 项通过）。桌面端同步新增 codex-usage 命令（提取 Codex 本地会话真实用量，实测 2791 条调用 / 5.7 亿 Token）。

影响：Codex（DeepSeek）真实用量无需代理或手动记录，桌面端提取 → 手机端导入即可可视化。

备注：导入为快照式（非实时），下一步做局域网一键同步。

## 日期：2026-08-08

完成：v0.2.7 发布——更新下载多线程分块加速（4 路并行，服务器不支持 Range 时自动回退单流），下载后大小 + SHA256 双重校验。

影响：国内用户更新包下载速度大幅提升。

备注：——

## 日期：2026-08-08

完成：建立手机端独立四件套文档（todo / memory / summary），与桌面端项目完全隔离；项目 AGENTS.md 已存在并符合规范。

影响：手机端进度独立记录，不再依赖桌面端文档。

备注：——

## 日期：2026-08-07

完成：手机 App 从骨架到可正式分发——v0.2.0 应用内更新；v0.2.1 更新提示增强；v0.2.2 稳定性优化（SHA256 校验、正式签名、按币种汇总、用量编辑删除、后台暂停刷新）；v0.2.3 自动清理临时包；v0.2.4 更新源兼容 version.json；v0.2.5 余额趋势图与刷新间隔可调；v0.2.6 修复重复更新弹窗。全部已发布 GitHub Release（正式签名 APK + SHA256）。

影响：用户可通过 App 内更新升级，无需手动重新下载。

备注：正式签名 keystore 与密码需用户自行备份，丢失无法覆盖升级。
